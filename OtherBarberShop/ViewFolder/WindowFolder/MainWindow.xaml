<Window x:Class="OtherBarberShop.ViewFolder.WindowFolder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OtherBarberShop.ViewFolder.WindowFolder"
        mc:Ignorable="d"
        Title="ГЛАВНОЕ ОКНО" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="5 10 5 0">
            <Button
                x:Name="WorkerButton"
                Click="WorkerButton_Click"
                Margin="0 0 0 5"
                Style="{StaticResource MenuButton}"
                Content="СОТРУДНИКИ"/>
            <Button 
                Name="RecordButton"
                Click="RecordButton_Click"
                Margin="0 5 0 5"
                Style="{StaticResource MenuButton}"
                Content="ЗАПИСЬ"/>
            <Button 
                Name="ServeseButton"
                Click="ServeseButton_Click"
                Margin="0 5 0 0"
                Style="{StaticResource MenuButton}"
                Content="УСЛУГИ"/>
        </StackPanel>
        <Frame
            x:Name="MainFrame"
            NavigationUIVisibility="Hidden"
            Grid.Column="1"
            Margin="10"/>
    </Grid>
</Window>
